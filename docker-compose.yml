version: "3.9"

services:
  cognix:
    build: .
    container_name: cognix-surface
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - cognix-data:/app/data
    restart: unless-stopped

volumes:
  cognix-data:
